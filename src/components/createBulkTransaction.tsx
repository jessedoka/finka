import { Button } from "./ui/button"

export function CreateBulkTransaction({ id }: { id: number }) {
    return (
        <div>
            <Button size="sm"> Add CSV file</Button>
        </div>
    )
}